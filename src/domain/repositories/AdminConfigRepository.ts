import { AdminConfig } from '../entities/AdminConfig';
import { BaseRepository } from './BaseRepository';

export interface AdminConfigRepository extends BaseRepository<AdminConfig> {
  // Add AdminConfig-specific methods...
}
